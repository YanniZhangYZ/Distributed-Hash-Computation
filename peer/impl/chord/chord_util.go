package chord

// querySuccessors queries a remote node about the successor of the given key, it can be used
// either when a new node joins the ring, or the node queries the object
func (c *ChordModule) querySuccessors(remoteNode string, key uint) string {
	return ""
}
